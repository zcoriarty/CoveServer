# CoveServer Configuration
# Values here are overridden by COVE_* environment variables.

[server]
host = "0.0.0.0"
port = 50051

[database]
url = "postgresql://postgres:replace-me@db.example.supabase.co:5432/postgres"
max_connections = 5
min_connections = 0

[storage]
endpoint = "https://example.supabase.co/storage/v1"
bucket = "media"
api_key = "replace-me"

[auth]
access_token_ttl_secs = 900        # 15 minutes
refresh_token_ttl_secs = 604800    # 7 days
jwt_secret = "change-me-in-production"

[crypto]
master_key_path = "./secrets/master.key"

[media]
max_upload_bytes = 52428800         # 50 MiB
max_video_duration_secs = 60
allowed_image_types = ["image/jpeg", "image/png", "image/webp"]
allowed_video_types = ["video/mp4", "video/quicktime"]

[push]
enabled = false
apns_key_id = ""
apns_team_id = ""
apns_bundle_id = ""
apns_private_key_path = "./secrets/AuthKey.p8"
default_environment = "development"
